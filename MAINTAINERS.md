# Оновлення шаблону

Не змінюйте файли студентів у `students/` (окрім `_example_*.py`, `.env.example`, `.gitkeep`).

Не комітьте реальні токени. `students/.env` у `.gitignore`.

Зміни ядра — у `app/`, `wsgi.py`, `SETUP.md`. Нові приклади — лише як `students/_example_*.py`.
